# CLAUDE.md

이 문서는 이 프로젝트에서 Claude가 작업할 때 반드시 지켜야 할 규칙입니다.

## 🔒 보안 — 가장 중요, 항상 지킬 것 (절대 어기면 안 됨)

다른 모든 규칙보다 우선합니다. 아래 항목은 예외 없이 지킵니다.

1. **비밀 키를 코드에 직접 쓰지 않는다.** API 키, 토큰, 비밀번호 등은 반드시 `.env` 같은 안전한 설정 파일에 넣고, 코드에서는 환경변수로만 참조한다.
2. **진짜 개인정보·대외비·실 데이터를 예시나 테스트에 쓰지 않는다.** 예시·테스트 데이터는 가짜 데이터만 사용한다.
3. **배포 관련 작업을 지시받으면, 먼저 코드와 설정에 비밀 키·개인정보·대외비 데이터가 섞여 있는지 검사한다.** 발견되면 즉시 작업을 멈추고 사용자에게 알린다. 문제가 없다고 확인되기 전에는 배포를 진행하지 않는다.

## 일하는 방식

1. 요청이 애매하면 멋대로 짐작하지 말고, 멈춰서 물어본다.
2. 시킨 것만 정확히 한다. 시키지 않은 기능이나 꾸밈을 마음대로 추가하지 않는다.
3. 고치라고 한 곳만 고친다. 무언가를 지우기 전에는 먼저 사용자에게 확인받는다.
4. 항상 한국어로 대화하고, 한글이 깨지지 않도록 신경 쓴다. 코드를 바꾼 뒤에는 무엇을 바꿨는지 짧게 설명한다.
5. 확실하지 않은 내용은 지어내지 말고 "확인 필요"라고 솔직하게 표시한다.
6. git·GitHub는 사용하지 않는다. 커밋이나 저장소 생성을 제안하지 않는다. 배포는 Vercel MCP로 한다.

## 데이터 규칙

7. 예시 데이터라도 코드에 직접 넣지 않고 Supabase에서 불러오도록 한다. 단, DB를 아직 연결하지 않은 화면(UI)만 만드는 단계에서는 `// 임시 데이터(나중에 교체)` 주석을 달고 임시로 넣는 것을 허용한다.
